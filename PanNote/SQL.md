![[Pasted image 20260420173653.png]]

在 Java 里的 JDBC 操作中，方法名通常和数据的**类型**直接相关：

- **`setInt(index, value)`**：用来设置**整数**（Internal/Integer）。
    
- **`setDouble(index, value)`**：用来设置**小数**（Double）。
    
- **`setString(index, value)`**：用来设置**字符串/文本**（String）。

java里的增删改查：
